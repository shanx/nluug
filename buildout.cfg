[buildout]
parts =
    django
    interpreter
eggs =
    ipython

[django]
recipe = djangorecipe
eggs = ${buildout:eggs}
projectegg = sytycnluug
settings = settings

[interpreter]
recipe = z3c.recipe.scripts
eggs = ${buildout:eggs}
interpreter = python
